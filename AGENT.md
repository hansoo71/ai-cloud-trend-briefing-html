# Agent Instructions

- Keep `src/`, `docs/index.html`, and dated `docs/YYYYMMDD/` in sync.
- Use canonical source URLs only; do not invent publication times.
- For Telegram delivery, provide GitHub Pages links with full `https://` markdown links.
- Preserve source extracts under `docs/source/` unless a public repo would expose private/copyrighted full text.
